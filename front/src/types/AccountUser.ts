export type AccountUser = {
  userId: string;
  userName: string;
  permissions?: string[];
  buildingId?: string;
  residencyId?: string;
  buildingName: string;
  groupName?: string;
  residencyName?: string;
};
