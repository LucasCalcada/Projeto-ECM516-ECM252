export interface VisitorAccess {
  id: string;
  buildingId: string;
  residencyId: string;
  residencyName?: string | null;
  residencyCode?: string | null;
  rg: string;
  cpf: string;
  name: string;
  entryAt: string;
}
